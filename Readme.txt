
1.Index.html
-- has a navigation bar, which will link to all the other pages
-- loads the json data,using javascipt and push it into rows of the table

2.main.css
--styles the common regions(menu bar, background color and font color) in all the pages

3.index.css
--styles the regions specific to the index.html page(table formatting and background images)

4.feed.json
--has the feed data(https://debug.gamerlink.gg/v2/gamerlink/feeds.json)

Important:
Successfully tested in Safari, Firefox and Internet Explorer(allow blocked contents, when pops up)